# Agent Notes: shekinah-app

## Scope
- Work only inside `shekinah-app/` unless explicitly asked otherwise.
- Keep changes minimal and targeted to the request.

## Setup
- Install deps: `npm install`
- Start Expo: `npm run start`

## Common Commands
- Android (Expo start): `npm run android`
- iOS (Expo start): `npm run ios`
- Web: `npm run web`
- Tests: `npm test`

## Guardrails
- Do not remove or overwrite APK artifacts unless explicitly requested.
- Run only relevant checks for files changed.
